name: Rust CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    name: Build, test, fmt, and clippy
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: clippy,rustfmt
          targets: x86_64-pc-windows-msvc

      - name: Cache cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Print Rust target info (debug)
        run: rustc --version && rustc --print cfg
        shell: bash

      - name: Build
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            cargo build --target x86_64-pc-windows-msvc --verbose
          else
            cargo build --verbose
          fi
        shell: bash

      - name: Run tests
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            cargo test --target x86_64-pc-windows-msvc --verbose
          else
            cargo test --verbose
          fi
        shell: bash

      - name: Check formatting (non-blocking)
        run: cargo fmt --all -- --check || echo "Code not properly formatted, continuing anyway..."
        shell: bash

      - name: Run Clippy
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            cargo clippy --target x86_64-pc-windows-msvc --all-targets --all-features -- -D warnings
          else
            cargo clippy --all-targets --all-features -- -D warnings
          fi
        shell: bash
